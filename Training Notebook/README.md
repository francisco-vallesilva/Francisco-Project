# Modified PyFoil
- Colab notebook used to export the trained ResNet-18 checkpoint (resnet18_airfoil.pth) to ONNX format
- Verifies the exported model produces identical output to the original PyTorch model before it's used in the live browser demo
- Run in Google Colab, top to bottom; outputs model.onnx
